<?xml version='1.0' encoding='iso-8859-1' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd" [ 
  <!ENTITY nbsp "&#160;">
] >

<issue num="16" status="Immediate">
<title>[PDTS] Append behavior underspecified if target is empty </title>
<section><sref ref="8.4.3 [path.append]"/></section>
<submitter>CH-11</submitter>
<date>20 Jan 2014</date>

<discussion>
<p>Is the added separator redundant in p1 /= p2, where p1 is empty? 
I.e. does the result start with a separator?</p>
  
  <note>2014-02-12 LWG/SG-3 Issaquah: The text is correct as written, however
  adding a note will clarify this and address the NB comment.</note>  
  
</discussion>

<resolution>
<p>Specify what behaviour is required.</p>
  <h4>Proposed wording:</h4>

  <p>
    <i>[2014-02-07: Beman Dawes comments:]</i> The second bullet item is supposed to deal with the empty() condition.
 </p>

  <p>
    <i>Change 8.4.3 [path.append]:</i>
  </p>
  <p>
    <i>Effects:</i>
  </p>
  <blockquote>
    Appends <code>path::preferred_separator</code> to <code>pathname</code> unless:<ul>
      <li>
        an added separator
        would be redundant, or
      </li>
      <li>would change a<del>n</del> relative path to a<del>n</del> absolute path
      <ins>[Note: An empty path is relative. --end note]</ins>, or</li>
      <li>
        <code>p.empty()</code>, or
      </li>
      <li>
        <code>*p.native().cbegin()</code> is a directory separator.
      </li>
    </ul>
    <p>
      Then appends <code>p.native()</code> to <code>pathname</code>.
    </p>
    </blockquote>
  </resolution>

</issue>
