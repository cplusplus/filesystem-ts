<?xml version='1.0' encoding='iso-8859-1' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd" [ 
  <!ENTITY nbsp "&#160;">
] >

<issue num="773" status="NAD">
<title>issues with random</title>
<section><sref ref="[rand.dist.uni]"/></section>
<submitter>P.J. Plauger</submitter>
<date>14 Jan 2008</date>

<discussion>
<ol>
<li>
<sref ref="[rand.dist.uni.int]"/> <tt>uniform_int</tt> constructor has changed the default
max constructor parameter from 9 (in TR1) to <tt>max()</tt>. The value
is arbitrary at best and shouldn't be lightly changed because
it breaks backward compatibility.
</li>

<li>
<sref ref="[rand.dist.uni.int]"/> <tt>uniform_int</tt> has a parameter <tt>param</tt> that you can
provide on construction or <tt>operator()</tt>, set, and get. But there
is not even a hint of what this might be for.
</li>

<li>
<sref ref="[rand.dist.uni.real]"/> <tt>uniform_real</tt>. Same issue as #2.
</li>
</ol>

<note>
Bellevue:
</note>

<blockquote><p>
NAD. Withdrawn.
</p></blockquote>
</discussion>

<resolution>
<p>
</p>
</resolution>

</issue>
