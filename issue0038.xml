<?xml version='1.0' encoding='iso-8859-1' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd" [ 
  <!ENTITY nbsp "&#160;">
] >

<issue num="38" status="New">
<title>[PDTS] Make certain functions noexcept and drop error_code version</title>
<section><sref ref="?"/></section>
<submitter>P.J. Plauger</submitter>
<date>30 Jan 2014</date>

<discussion>
<p>
  exists(const path&) should be noexcept (drop error_code version).
  
  is_*(const path&) should be noexcept (drop error_code version).<br/>
  status(const path&) should be noexcept (drop error_code version).<br/>
  symlink_status(const path&) should be noexcept (drop error_code version).<br/>
  file_status::status() should be noexcept (drop error_code version).<br/>
  file_status::symlink_status() should be noexcept (drop error_code version).<br/>
  unique_path(const path&) should be noexcept (drop error_code version).
</p>
</discussion>

<resolution>
<p></p>
</resolution>

</issue>
